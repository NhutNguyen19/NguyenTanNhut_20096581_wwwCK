server:
  port: 8080
  servlet:
    context-path: /api
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/www_data_btlon
    username: root
    password: sapassword
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
mailServer:
  host: smtp.gmail.com
  port: 587
  email: mailsender.tutortials.b2a@gmail.com
  password: Admin@123
  protocol: smtp
  isSSL: false